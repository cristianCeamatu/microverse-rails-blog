<p id="notice"><%= notice %></p>

<h1>Authors</h1>

<table>
  <thead>
    <tr>
      <th>Username</th>
      <th class="pl-2">Email</th>
      <th class="pl-2" colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @authors.each do |author| %>
      <tr>
        <td><%= author.username %></td>
        <td class="pl-2"><%= author.email %></td>
        <td class="pl-2"><%= link_to 'Show', author %></td>
        <td class="pl-2"><%= link_to 'Edit', edit_author_path(author) %></td>
        <td class="pl-2"><%= link_to 'Destroy', author, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Author', new_author_path %>
